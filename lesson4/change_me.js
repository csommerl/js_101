// https://launchschool.com/lessons/60e10aa5/assignments/73c01de6

// PROBLEM:

// Given a string, write a function changeMe which returns the same
// string but with all the words in it that are palindromes uppercased.

// changeMe("We will meet at noon") === "We will meet at NOON"
// changeMe("No palindromes here") === "No palindromes here"
// changeMe("") === ""
// changeMe("I LOVE my mom and dad equally") === "I LOVE my MOM and DAD equally"

// My test cases
// changeMe("Mom is not a palindrome.") === "I LOVE my MOM and DAD equally"

// inputs: string
// output: string (not the same object)
// rules:
  // Explicit
    // Every palindrome in a string must be converted to upper case
    // palindromes are case sensitive
  // Implicit
    // if the string is an empty string, return empty string

    
